/* eslint-disable no-unused-vars */
export enum colors {
   FONT = '#1a1a1a'
}

export enum shadows {
   NORMAL = '3px 3px 20px rgba(0, 0, 0, 0.1)'
}

export enum breakpoints {
   FULL = '100%',
   XL = "1440px'",
   LG = '1024px',
   MD = '768px',
   SM = '425px',
   XS = '320px'
}
