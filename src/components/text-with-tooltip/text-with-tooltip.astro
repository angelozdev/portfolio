---
interface Props extends astroHTML.JSX.HTMLAttributes {
  tooltip: string;
}

const { tooltip, class: className, ...rest } = Astro.props;
---

<span {...rest} class:list={["with-tooltip", className]} data-tooltip={tooltip}>
  <slot />
</span>
